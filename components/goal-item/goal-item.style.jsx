import { StyleSheet } from "react-native";

const GoalStyles = StyleSheet.create({
  goalContainer: {
    padding: 16,
    backgroundColor: "#ddd",
    marginVertical: 8,
  },
});

export default GoalStyles;
